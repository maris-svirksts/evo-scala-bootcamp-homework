package com.evolution.bootcamp.containers.integration
/*
import java.io.File

import com.dimafeng.testcontainers.{Container, DockerComposeContainer, ForAllTestContainer}
import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.must.Matchers.be
import org.scalatest.matchers.should.Matchers.convertToAnyShouldWrapper
import org.scalatest.verbs.ShouldVerb

class MailAgentSpec extends AnyFlatSpec with ShouldVerb with ForAllTestContainer  {
  override def container: Container = DockerComposeContainer( new File("integration/test-stack/docker-compose-mailcatcher.yml") )

  "MailAgent" should "send example email in configured environment" in {
    val ma = new MailAgent()
    ma.sendExampleEmail("Test Example", "Example body.") should be (Right(()))
  }
}
*/
